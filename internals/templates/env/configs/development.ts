import { EnvironmentVariables } from '../types';

/**
 * Define your development environment variables here.
 *
 * If you require, you can add variables to more environments
 * by creating more [ENVIRONMENT_NAME].ts files in this directory.
 */
const e: EnvironmentVariables = {
  api: null,
};

export default e;
