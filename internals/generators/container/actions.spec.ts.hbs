import { {{ properCase name }}ActionType } from '../store/constants';
import { createDefaultAction } from '../store/actions';

describe('{{ properCase name }} actions', () => {
  describe('Default Action', () => {
    it('has a type of DEFAULT_ACTION', () => {
      const expected = {
        type: {{ properCase name }}ActionType.DEFAULT_ACTION,
      };
      expect(createDefaultAction()).toEqual(expected);
    });
  });
});
